const express = require("express");
const app = express();
// connect with the postgrees db
app.use(express.json());
// call the db postgress here
// const {sequelize , testDbConnection} = require('./config/DbConnection')
// testDbConnection();


